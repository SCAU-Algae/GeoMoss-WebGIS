# 🛰️ WebGIS 3.0 全栈开发任务指令

> **[❗重要指令]**：本任务必须严格遵守 `/docs/Force_prompt.md` 中的《开发维护日志规范》。任务完成后，必须自动生成维护日志并更新项目三级目录结构。

---

## 1. 问题/需求描述 (Context & Problem)

* **当前现象**：

* **痛点分析**：
    1.  **同步逻辑失效**：
    2.  **配置硬编码**：
    3.  **链路阻断**：

---

## 2. 预期效果 (Expected Outcome)

### 📂 配置持久化与全局同步
* **后端要求**：
* **动态拉取**：

### 👥 灵活的配额管理台
* **CRUD 实现**：
* **拒绝硬编码**：

### ⚡ 额度消耗与熔断机制
* **实时校验**：
* **事务性更新**：
* **前端交互**：

---

## 🛡️ 执行约束 (Constraints)

1.  **禁止越权**：Agent 严禁执行 `git push` 或 `git commit`，仅负责准备代码变更。
2.  **日志先行**：任务结束时，必须在 `/docs/` 下创建日期命名的 `.md` 日志。
3.  **结构同步**：强制更新根目录、`frontend`、`backend` 三个 README 中的文件树注释。
4.  **禁止冗余**：除 `/docs/yy-mm-dd/` 目录外，禁止在任何地方新建说明文档或 README。

---

**请根据上述逻辑，对全栈代码进行深度审计并给出修复方案。**